# Public base URL of the R2 bucket (or CDN in front of it) that holds the installers
# and latest.json. The download page fetches `${VITE_DOWNLOADS_BASE_URL}/latest.json`.
VITE_DOWNLOADS_BASE_URL=https://downloads.blink.wolkenassistent.de
