# Desktop app environment — copy to `.env` (gitignored) in this folder.
# Loaded at startup by the Rust core (dotenvy). Real exported env vars still win.

# OpenAI API key for the "Optimize with AI" button in copy capture.
OPENAI_API_KEY=
