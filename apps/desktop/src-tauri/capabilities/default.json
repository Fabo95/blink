{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for Blink's main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
