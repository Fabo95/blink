{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // WebCrypto module — only the DOM lib globals, never ambient @types/node
    // (which would make Uint8Array conflict with BufferSource).
    "types": []
  },
  "include": ["src"]
}
