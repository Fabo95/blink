packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  # Unused optional peers of better-auth (prisma / sqlite adapters we don't use) — don't
  # build them, and don't error on their ignored build scripts.
  '@prisma/client': false
  better-sqlite3: false
  esbuild: true
